### Problem

#### Given a string, return a new string with the reversed order of characters.

### Example
```
reverse('banana') === 'ananab'
reverse('hello') === 'olleh'
reverse('Apple') === 'elppA'
```